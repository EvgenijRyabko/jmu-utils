JMU Utils npm package

This is a npm package for LGPU microservice projects.

Current package utils list:

- **Request parser** for nest.js microservice (Transform string request to a typed obj of express Request type).
- **params-meta decorator** (A extension for request parser to set params template metadata. Needed to correctly parse params).
